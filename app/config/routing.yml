user:
    resource: "@UserBundle/Controller/"
    type:     annotation
    prefix:   /

ticketing:
    resource: "@TicketingBundle/Controller/"
    type:     annotation
    prefix:   /

login_check:
    path: /login_check

logout:
    path: /logout
